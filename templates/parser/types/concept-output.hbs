<h3>{{ item.name }}</h3>

<dl>
	<dt>Type</dt>
	<dd>{{ item.type }}</dd>
	{{#if related.length}}
		<dt>Related Features</dt>
		{{#each related}}
			<dd>{{ name }}</dd>
		{{/each}}
	{{/if}}
</dl>

<hr>

{{{ enriched.description }}}
