<h3>{{ item.name }}</h3>

<dl>
	<dt>Type</dt>
	<dd>{{ item.type }}</dd>
	{{#if related.length}}
		<dt>Related Features</dt>
		{{#each related}}
			<dd><strong>{{ name }}</strong></dd>
		{{/each}}
	{{/if}}
</dl>

<hr>

{{{ enriched.description }}}
